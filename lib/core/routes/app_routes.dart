/// Nombres de rutas — un solo lugar, sin strings sueltos en la app.
class AppRoutes {
  AppRoutes._();

  static const String login = '/login';
  static const String register = '/register';
  static const String opticsList = '/optics';
  static const String opticsForm = '/optics/form';
}
